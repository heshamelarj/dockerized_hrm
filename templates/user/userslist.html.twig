{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

    {% block sidebar %}

        {% include 'includes/sidebar.html.twig' %}
    {% endblock %}

{% block body %}
    <div class="">

    {% for user in users %}

        <ul class="list-none ">

            <li class=" w-100  flex border-b border-blue-400 pb-2 hover:border-red-200">
                {#<a class=" text-decoration-none w-full hover:bg-blue-200">#}

                <div class="w-1/3">
                    <h3 class="text-gray-600 text-3xl  uppercase">{{ user.nom }}</h3>
                    <i class="text-gray-500  border-b-2 border-blue-200 ">{{ user.som }}</i>

                </div>
                <div class="ml-auto my-auto">
                    <a class="text-decoration-none block" target="_blank"
                       href={{ path('generate-pdf',{'som': user.som }) }}>
                        <span class="fas fa-folder text-green-300 "></span>
                        <i class="text-green-300 p-1">Attestation Travaill</i>
                    </a>
                    <a type="button" target="_blank" class="text-decoration-none block" data-toggle="modal"
                            data-target="#travelaAutorisationModal" href={{ path('list-users') }}>
                        <span class="fas fa-file text-blue-400"></span>
                        <i class="text-blue-400 p-1">Autrisations </i>
                    </a>

                </div>
                {#</a>#}
            </li>
        </ul>
        {% include 'includes/_demand_autorisation.html.twig' %}
    {% endfor %}
    </div>




{% endblock %}