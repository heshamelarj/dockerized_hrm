<div class="navbar bg-white mb-2">
    <nav class="navbar navbar-expand-lg navbar-light  bg-white w-100  fixed-top">

        <div class="collapse navbar-collapse " id="navbarNav">
            <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                <li class="nav-item active">
                </li>
            </ul>
        </div>
        {% if is_granted('ROLE_USER') %}
        <a class="text-decoration-none p-2 text-gray-800 hover:text-gray-900  hover:text-black bg-blue-700" href={{path('logout')}}>Se deconnecter<span class="sr-only"></span></a>
        {% elseif route_name == 'login' and is_granted('ROLE_ADMIN') %}
        <a class="nav-link" href={{path('register')}}>S'inscrire<span class="sr-only"></span></a>
        {% elseif route_name != 'login'  %}
        <a class="nav-link" href={{path('login')}}>Se connecter<span class="sr-only"></span></a>
        {% endif %}
    </nav>
</div>
